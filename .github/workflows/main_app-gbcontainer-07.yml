# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - app-gbcontainer-07

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: crkkm7.azurecr.io/
          username: ${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_8377D39BCC9A4CAC977339EE690BCD05 }}
          password: ${{ secrets.AZUREAPPSERVICE_CONTAINERPASSWORD_235B23E4B9FA41D4B1939E539759F656 }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: crkkm7.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_8377D39BCC9A4CAC977339EE690BCD05 }}/guestbook-app:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    needs: build
    
    steps:
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_D2312975B1C243B3B1395CDED7A53901 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_4E1E40527F544FC9A449A79A0EE35BAB }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_D3B8E694ADA54DBEB5C0E9E781CAB5B2 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'app-gbcontainer-07'
          slot-name: 'Production'
          images: 'crkkm7.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_8377D39BCC9A4CAC977339EE690BCD05 }}/guestbook-app:${{ github.sha }}'
          